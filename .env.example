# Flask Configuration
FLASK_ENV=development
PORT=5000

# Database Configuration
# For local development (SQLite - default if not set):
# DATABASE_URL=sqlite:///crud_dashboard.db

# For MySQL (production):
# DATABASE_URL=mysql+pymysql://username:password@host:port/database_name
# Example: DATABASE_URL=mysql+pymysql://root:password@localhost:3306/crud_dashboard

# For hosting platforms (they usually set DATABASE_URL automatically):
# Heroku, Railway, Render will auto-configure this
