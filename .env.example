# Flask Configuration
FLASK_ENV=development
PORT=5000

# Database Configuration
# For local development (SQLite - default if not set):
# DATABASE_URL=sqlite:///crud_dashboard.db

# For PostgreSQL (Render, Heroku, Railway):
# DATABASE_URL=postgresql://username:password@host:port/database_name
# Example: DATABASE_URL=postgresql://user:pass@localhost:5432/crud_dashboard

# For MySQL (if needed):
# DATABASE_URL=mysql+pymysql://username:password@host:port/database_name

# Note: Render automatically sets DATABASE_URL for PostgreSQL databases
